<%= render partial: 'shared/title', locals: { title: 'Viewing Expense Detail' } %>

<%= render partial: 'form', locals: { read_only: true } %>

<%= link_to 'Edit', edit_expense_detail_path(@expense_detail) %>