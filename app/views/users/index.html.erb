<%= render partial: 'shared/title', locals: { title: 'Listing Users' } %>

<table id="users" class="display" data-source="<%= users_url(format: "json") %>">
	<thead>
		<tr>
			<th>Id</th>
			<th>Name</th>
			<th>Company</th>
			<th>User type</th>
		</tr>
	</thead>

	<tbody>
	</tbody>
</table>

<br />
