<%= render partial: 'shared/title', locals: { title: 'New User' } %>

<% if !@user.company_id? %>
  <% @user.company_id = current_user().company_id %>
<% end %>
<%= render partial: 'form', locals: { read_only: false } %>