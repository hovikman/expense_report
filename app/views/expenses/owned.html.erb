<h1>Listing Expenses Owned</h1>

<table id="expenses_owned" class="display">
	<thead>
		<tr>
			<th>Id</th>
			<th>User</th>
			<th>Submit date</th>
			<th>Purpose</th>
			<th>Expense status</th>
		</tr>
	</thead>

	<tbody>
		<% @expenses.each do |expense| %>
		<tr>
			<td><%= expense.id %></td>
			<td><%= expense.user.name %></td>
			<td><%= expense.submit_date %></td>
			<td><%= expense.purpose %></td>
			<td><%= expense.expense_status.name %></td>
		</tr>
		<% end %>
</table>

<br />
<br />
<br />

<%= link_to 'Submitted Expenses', submitted_expenses_path %>