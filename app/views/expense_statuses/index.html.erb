<h1>Listing expense_statuses</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @expense_statuses.each do |expense_status| %>
  <tr>
    <td><%= expense_status.name %></td>
    <td><%= link_to 'Show', expense_status %></td>
    <td><%= link_to 'Edit', edit_expense_status_path(expense_status) %></td>
    <td><%= link_to 'Destroy', expense_status, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Expense status', new_expense_status_path %>
