<%= render partial: 'shared/title', locals: { title: 'Viewing Expense Type' } %>

<%= render partial: 'form', locals: { read_only: true } %>

<%= link_to 'Edit', edit_expense_type_path(@expense_type) %> 